# Assignment: Lab 1 - MATLAB Direct Calculations

**Topic:** Introduction to MATLAB interface and basic operations in direct calculation mode.

## Task 1: Arithmetic Expression
Calculate the following expression with the specified constants:
- **Expression:** `abs(A + B + C) / D`
- **Constants:** - `A = sin(0)`
  - `B = cos(0)`
  - `C = log(1)`
  - `D = exp(1)`
- **Format:** `short e`

## Task 2: Complex Numbers
1. Create a complex number `z = a + bi`, where:
   - `a` = number of letters in class list.
   - `b` = number of letters in class name.
2. Perform the following operations:
   - Determine the real and imaginary parts.
   - Calculate the magnitude (absolute value).
   - Find the complex conjugate.
   - Compute the expression: `sin(a + bi) + cos(a - bi)`.